import Section from '../components/Section';

const NotFound = () => {
    return (
        <main>
            <Section>
                <p> Notfound Content </p>
            </Section>
        </main>
    );
}

export default NotFound;