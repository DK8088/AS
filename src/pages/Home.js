import Section from '../components/Section';

const Home = () => {
    return (
        <main>
            <Section>
                <p> Homepage Content </p>
            </Section>
        </main>
    );
}

export default Home;