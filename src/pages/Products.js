import Section from '../components/Section';

const Products = () => {
    return (
        <main>
            <Section>
                <p> Masala Content </p>
            </Section>
        </main>
    );
}

export default Products;